import { SourcesConfig } from "../../types/config.js";
import { KeyedDataset } from "../../types/dataparsers.js";

export const garminFit = (config: SourcesConfig): KeyedDataset => {
    config;
    return {}
    
}
